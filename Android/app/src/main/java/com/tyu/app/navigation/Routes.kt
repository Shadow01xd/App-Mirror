package com.tyu.app.navigation

enum class TyuRoute {
    Welcome, Permissions, Nearby, Qr, Connecting, ConnectionError, Home,
    Monitor, MonitorActive, Mirror, MirrorActive, Bypass,
    Camera, CameraActive, Microphone, MicrophoneActive,
    Storage, Transfer, TransferProgress, Device, Sessions, Settings,
}
